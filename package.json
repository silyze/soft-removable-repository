{
  "name": "@silyze/soft-removable-repository",
  "version": "1.0.0",
  "description": "Soft removable extensions for @mojsoski/ormm",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "keywords": [],
  "author": "Mihail Mojsoski",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "@types/pg": "^8.15.4",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "watch": "npx tsc --watch",
    "build": "npx tsc",
    "prepare": "npm run build",
    "version": "git add -A lib",
    "postversion": "git push && git push --tags",
    "start": "node dist/example.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@mojsoski/ormm": "^1.0.5",
    "@silyze/batch-repository": "^1.0.1"
  }
}
